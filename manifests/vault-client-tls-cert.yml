---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-client-tls
  namespace: default
spec:
  secretName: vault-client-tls
  issuerRef:
    name: shared-vault-issuer
    kind: ClusterIssuer
  commonName: VAULT CLIENT CERT
...
